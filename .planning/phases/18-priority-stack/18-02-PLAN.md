---
phase: 18-priority-stack
type: execute
domain: itir-suite
---

<objective>
Plan the regex → deterministic tokenizer migration for the shared compression engine.

Purpose: Ensure the core tokenization transition is deterministic, reversible, and parity-tested.
Output: A migration plan doc with decisions, steps, and verification checkpoints.
</objective>

<execution_context>
~/.codex/skills/get-shit-done/workflows/execute-phase.md
./summary.md
~/.codex/skills/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@SensibLaw/docs/tokenizer_contract.md
@docs/planning/compression_engine.md
@docs/planning/sl_lce_profile_followthrough_20260208.md
@SensibLaw/docs/lexeme_layer.md
@docs/planning/wiki_timeline_extraction_gwb_20260211.md
</context>

<tasks>

<task type="auto">
  <name>Draft tokenizer migration plan</name>
  <files>docs/planning/tokenizer_migration_plan_20260306.md</files>
  <action>Write a plan covering: tokenizer choice candidates, versioning scheme, canonical token stream decision impacts, parity checkpoints (graph hydration snapshots for wiki timeline routes), migration steps, rollback strategy, and required tests. Keep it deterministic and non-semantic.</action>
  <verify>rg -n "tokenizer_migration_plan_20260306" docs/planning</verify>
  <done>Plan doc exists with explicit steps and verification checkpoints.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <decision>Tokenizer replacement path</decision>
  <context>Choose the deterministic tokenizer to replace regex at Layer‑1 while preserving stable offsets and parity.</context>
  <options>
    <option id="spacy-deterministic">
      <name>spaCy deterministic config</name>
      <pros>Already used in pipeline; easier integration path.</pros>
      <cons>Requires strict version pinning and model parity controls.</cons>
    </option>
    <option id="icu-udpipe">
      <name>ICU/UDPipe deterministic tokenizer</name>
      <pros>Strong language coverage and stable tokenization rules.</pros>
      <cons>New integration surface and additional tooling cost.</cons>
    </option>
  </options>
  <resume-signal>Select: spacy-deterministic or icu-udpipe</resume-signal>
</task>

<task type="auto">
  <name>Define parity checkpoint artifacts</name>
  <files>docs/planning/tokenizer_migration_plan_20260306.md</files>
  <action>Specify the exact checkpoint artifacts to compare (wiki timeline hydration payloads for `/graphs/wiki-timeline`, `/graphs/wiki-timeline-aoo`, `/graphs/wiki-timeline-aoo-all`), and the commands or scripts that generate them. Note that parity must be byte-identical.</action>
  <verify>rg -n "wiki-timeline|parity|checkpoint" docs/planning/tokenizer_migration_plan_20260306.md</verify>
  <done>Parity checkpoints are explicit and tied to the migration steps.</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `docs/planning/tokenizer_migration_plan_20260306.md` exists and is linked from the priority sequence plan.
- [ ] Tokenizer replacement path decision recorded.
- [ ] Parity checkpoint artifacts are explicit and reproducible.
</verification>

<success_criteria>

- Migration plan is complete and deterministic.
- Replacement path is decided with rationale.
- Parity verification is executable and unambiguous.
  </success_criteria>

<output>
After completion, create `.planning/phases/18-priority-stack/18-02-SUMMARY.md`:

# Phase 18 Plan 02: Tokenizer Migration Planning Summary

**[One-liner summary of what was decided and planned]**

## Accomplishments

- [Key outcome 1]
- [Key outcome 2]

## Files Created/Modified

- `docs/planning/tokenizer_migration_plan_20260306.md` - Tokenizer migration plan

## Decisions Made

[Decision summary or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Ready to execute tokenizer migration plan checkpoints.
</output>
